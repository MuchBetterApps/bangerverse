{
  "name": "banger-pulse",
  "version": "0.4.0",
  "description": "Lightweight desktop notifications for Banger mail and one time codes",
  "private": false,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "frontend:dev": "vite --host 127.0.0.1 --port 1420",
    "frontend:build": "vite build",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^7.2.7"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "lucide": "^0.468.0"
  }
}
